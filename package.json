{
    "name": "skild",
    "version": "0.0.1",
    "private": true,
    "description": "The npm for Agent Skills — Discover, install, manage, and publish AI Agent Skills with ease.",
    "scripts": {
        "dev": "pnpm --filter web dev",
        "dev:web": "pnpm --filter web dev",
        "dev:cli": "pnpm --filter ./packages/cli dev",
        "build": "pnpm -r --filter './apps/*' --filter './packages/*' build",
        "build:web": "pnpm --filter web build",
        "build:cli": "pnpm --filter ./packages/cli build",
        "typecheck": "pnpm -r --if-present typecheck",
        "typecheck:cli": "pnpm --filter ./packages/cli typecheck",
        "preview": "pnpm --filter web preview",
        "lint": "pnpm -r --if-present lint",
        "test": "pnpm -r --if-present test",
        "clean": "rm -rf apps/*/dist packages/*/dist node_modules/.cache",
        "cli": "node packages/cli/dist/index.js",
        "release:cli": "pnpm -C packages/cli release"
    },
    "keywords": [
        "agent-skills",
        "ai",
        "npm",
        "cli",
        "package-manager"
    ],
    "author": "Peiiii",
    "license": "MIT",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/Peiiii/skild.git"
    },
    "engines": {
        "node": ">=18",
        "pnpm": ">=8"
    },
    "packageManager": "pnpm@10.12.3"
}
