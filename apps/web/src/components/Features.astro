---

---

<section class="max-w-5xl mx-auto px-6 py-20 border-t border-white/5">
    <div class="grid md:grid-cols-3 gap-6 reveal">
        <!-- Multi-platform -->
        <div
            class="glass rounded-2xl p-6 border border-white/10 hover:border-indigo-500/30 transition-colors"
        >
            <div class="text-3xl mb-4">🌐</div>
            <h3 class="text-xl font-bold mb-2">Multi-platform</h3>
            <p class="text-white/40 text-sm leading-relaxed">
                Claude, Codex, Copilot, Antigravity, OpenCode — one tool manages
                all. Switch with
                <code class="text-indigo-400">-t opencode</code>.
            </p>
        </div>

        <!-- Skillsets (highlighted) -->
        <div
            class="glass rounded-2xl p-6 border border-purple-500/30 bg-purple-500/5 hover:border-purple-500/50 transition-colors relative overflow-hidden"
        >
            <div
                class="absolute top-0 right-0 w-24 h-24 bg-purple-500/10 blur-2xl"
            >
            </div>
            <div class="relative">
                <div class="flex items-center gap-2 mb-4">
                    <span class="text-3xl">🎁</span>
                    <span
                        class="text-[10px] font-bold uppercase tracking-wider text-purple-400 bg-purple-500/20 px-2 py-0.5 rounded"
                        >Unique</span
                    >
                </div>
                <h3 class="text-xl font-bold mb-2 text-purple-200">
                    Skillsets
                </h3>
                <p class="text-white/40 text-sm leading-relaxed">
                    One pack = many skills. Install complete toolkits with a
                    single command.
                </p>
            </div>
        </div>

        <!-- npm-like -->
        <div
            class="glass rounded-2xl p-6 border border-white/10 hover:border-indigo-500/30 transition-colors"
        >
            <div class="text-3xl mb-4">📦</div>
            <h3 class="text-xl font-bold mb-2">npm-like</h3>
            <p class="text-white/40 text-sm leading-relaxed">
                <code class="text-indigo-400">install</code> ·
                <code class="text-indigo-400">publish</code> ·
                <code class="text-indigo-400">search</code> — if you know npm, you
                know skild.
            </p>
        </div>
    </div>
</section>
